import { DoesArrayHaveValuesPipe } from './does-array-have-values.pipe';

describe('DoesArrayHaveValuesPipe', () => {
  it('create an instance', () => {
    const pipe = new DoesArrayHaveValuesPipe();
    expect(pipe).toBeTruthy();
  });
});
